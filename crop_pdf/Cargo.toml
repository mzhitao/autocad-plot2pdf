[package]
name = "crop_pdf"
version = "0.1.0"
edition = "2021"

[profile.release]
opt-level = "z"     # 优化体积
lto = true          # 链接时优化
codegen-units = 1   # 单代码单元，进一步优化
strip = true        # 去掉符号表
panic = "abort"     # 减小异常处理体积

[dependencies]
lopdf = "0.41"
serde_json = "1"
